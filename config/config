# Custom commit configuration.
# Set custom=true and fill a value below to pin that repository.
# Empty values use the selected branch's latest commit.

# Enable custom commit pinning (true/false)
custom=false

# SUSFS commit hash for each branch (used only when custom=true)
gki-android12-5.10=
gki-android13-5.15=
gki-android14-6.1=
gki-android15-6.6=
gki-android16-6.12=

# KittiSU commit hash (used only when custom=true)
kittisu=
